<h1>Add New Bulk Discount</h1>
<%= form_with url: merchant_bulk_discounts_path(@merchant), method: :post, local: true do |form| %>
<%= form.label :markdown %>
  <%= form.text_field :markdown %>
<%= form.label :threshold %>
  <%= form.text_field :threshold %>
<%= form.submit "Submit" %>
<% end %>
